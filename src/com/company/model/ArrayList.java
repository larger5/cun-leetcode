package com.company.model;

public class ArrayList {
    /**
     * 循环遍历
     *
     * @param arr 数组
     */
    private void loop(int[] arr) {
        for (int i = 0; i < arr.length; i++) {
            // 迭代访问 arr[i]
        }
    }
}
